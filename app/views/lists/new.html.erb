<h1>Add a new List</h1>

<div class="container">
  <%= simple_form_for(@list) do |f|%>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>

<%= link_to "Back to lists", lists_path, class: "btn btn-success" %>
</div>

<h1><%= @list.name %></h1>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %> - <%= bookmark.comment %> <%= link_to "🗑️", bookmark_path(bookmark),
      data: { turbo_method: :delete, turbo_confirm: "Delete bookmark?" } %></li>
  <% end %>
</ul>
<%= link_to "Add movie", new_list_bookmark_path(@list), class: "btn btn-success" %>
<%= link_to "Back to lists", lists_path, class: "btn btn-success" %>
